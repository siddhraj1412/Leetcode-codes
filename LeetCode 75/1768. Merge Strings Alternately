class Solution:
    def mergeAlternately(self, word1: str, word2: str) -> str:
        j=""
        for i in range(min(len(word1),len(word2))):
            j+=word1[i]+word2[i]
        return j+word1[i+1:]+word2[i+1:]
